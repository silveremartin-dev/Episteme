apiVersion: apps/v1
kind: Deployment
metadata:
  name: episteme-server
  namespace: episteme
  labels:
    app: episteme-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: episteme-server
  template:
    metadata:
      labels:
        app: episteme-server
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
    spec:
      containers:
        - name: server
          image: episteme/server:latest
          ports:
            - containerPort: 50051
              name: grpc
            - containerPort: 9090
              name: metrics
            - containerPort: 8080
              name: rest
          env:
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: episteme-secrets
                  key: JWT_SECRET
          envFrom:
            - configMapRef:
                name: episteme-config
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "2000m"
              memory: "2Gi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: episteme-server
  namespace: episteme
spec:
  selector:
    app: episteme-server
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
    - name: rest
      port: 8080
      targetPort: 8080
    - name: metrics
      port: 9090
      targetPort: 9090
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: episteme-server-lb
  namespace: episteme
spec:
  selector:
    app: episteme-server
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
    - name: rest
      port: 80
      targetPort: 8080
  type: LoadBalancer
